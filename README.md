# Homebrew Tap for Nexus

This repository hosts the Homebrew formula for Nexus.

## Install

```bash
brew tap edgeopslabs/nexus
brew install nexus
```

## Update Formula

Replace the URL and SHA256 in `Formula/nexus.rb` whenever you cut a new release.
